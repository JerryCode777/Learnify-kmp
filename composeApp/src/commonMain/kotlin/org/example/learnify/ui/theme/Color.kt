package org.example.learnify.ui.theme

import androidx.compose.ui.graphics.Color

// Kotlin Multiplatform + Antigravity inspired colors (LIGHT THEME)
// Primary - Deep Blue tones (elegant and professional)
val Primary = Color(0xFF0D47A1)  // Deep Blue
val PrimaryLight = Color(0xFFE3F2FD)  // Very light blue for containers
val PrimaryDark = Color(0xFF01579B)  // Darker deep blue

// Secondary - Complementary accent
val Secondary = Color(0xFF1976D2)  // Medium Blue accent
val SecondaryLight = Color(0xFFBBDEFB)
val SecondaryDark = Color(0xFF0D47A1)

// Tertiary - Additional accent
val Tertiary = Color(0xFF00BFA5)  // Teal accent
val TertiaryLight = Color(0xFFE0F7F4)
val TertiaryDark = Color(0xFF00897B)

// Error
val Error = Color(0xFFD32F2F)
val ErrorLight = Color(0xFFFFEBEE)
val ErrorDark = Color(0xFFC62828)

// LIGHT backgrounds - Antigravity style (clean white)
val Background = Color(0xFFFAFAFA)  // Very light gray background
val Surface = Color(0xFFFFFFFF)  // Pure white surface
val SurfaceVariant = Color(0xFFF5F5F5)  // Light gray variant

// Text colors for LIGHT theme (black text)
val OnPrimary = Color(0xFFFFFFFF)  // White text on primary purple
val OnBackground = Color(0xFF1A1A1A)  // Almost black text
val OnSurface = Color(0xFF1A1A1A)  // Almost black text
val OnSurfaceVariant = Color(0xFF616161)  // Gray text

// Divider
val Outline = Color(0xFFE0E0E0)  // Light gray divider
